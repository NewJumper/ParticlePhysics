#ifndef SETTINGS_H
#define SETTINGS_H

#include "raylib.h"

const Vector2 windowSize = { 1280, 720 };
const Vector2 borderCenter = { windowSize.x / 2.0f, windowSize.y / 2.0f };
const int borderRadius = 300;

extern bool showVectors;

#endif
